# Used only for the secret-video corner gesture (see PiDashboard.py,
# play_secret_video()). The touchscreen driver emits taps as MBTN_LEFT
# (same as every other tap this app handles), so this lets a viewer close
# the video by tapping anywhere on it instead of being stuck until it ends.
MBTN_LEFT quit
